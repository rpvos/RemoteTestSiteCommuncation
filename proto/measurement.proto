syntax = "proto3";

package RemoteTestSite;

import "proto/measurement_info.proto";
import "proto/timestamp.proto";

message Measurement {
  optional Timestamp timestamp = 1;
  optional uint32 value = 2;
  optional MeasurementInfo info = 3;
}